/** Single source of truth for app identity shown across the UI (sidebar, footer,
 *  profile, About dialog). Bump these on release. */
export const APP_VERSION = "1.0";

/** Human-readable build/release marker. */
export const BUILD_DATE = "2026.06";
